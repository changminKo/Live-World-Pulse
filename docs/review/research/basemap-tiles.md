[실측] OpenFreeMap 스타일 5종(liberty/bright/positron/dark/fiord) 전부 HTTP 200 fetch — dark 스타일 실존(bg rgb(12,12,12), 47 layers), 타일은 tiles.openfreemap.org/planet 버전드 pbf + Cloudflare, 정책 원문 '뷰/요청 무제한·상업 허용·키 불요'(단 SLA 없음, 100k req/s 생존 실적). Protomaps planet pmtiles는 HEAD 실측 137.37GB(20260817 빌드)로 R2 self-host 시 ~/월 저장+Worker /월+운영 부담, 호스팅 API는 상업=스폰서십이라 포트폴리오 영구 무료 지향에 불리; NASA GIBS Black Marble은 CORS * + HTTP 200 실측(maxzoom 8)이라 저줌 야간 지구 배경 하이브리드 가능하나 '브라우저 직접 fetch 예외' 규칙 추가 결정 필요. 판정: (a) OpenFreeMap 공개 인스턴스 추천 (b) 다크 저작 시작점 = styles/dark JSON 포크 후 Maputnik 편집·리포 커밋 (c) Phase -1 스파이크 URL = https://tiles.openfreemap.org/styles/dark + projection globe, sky 스펙 금지(mercator 전용, maplibre #5230)·라벨 과대/차폐 이슈 스파이크 체크리스트 포함. 남은 것 없음 — 상세는 reportPath 참조.