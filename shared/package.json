{
  "name": "lwp-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Live World Pulse 공유 계약 — PLAN §5 데이터 모델 + §6 시간 의미론 + §8.5 URL State + §8.6 R2 파일 계약 (의존성 0 순수 타입+함수)",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
